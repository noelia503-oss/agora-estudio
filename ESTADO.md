# Estado del proyecto

**Fecha de actualización:** 2026-09-25
**Estado general:** MVP local implementado y paquete autónomo de escritorio para Mac generado; compilación correcta. La fase 8 está implementada y espera la configuración OAuth de Google para probarse entre dispositivos.

**App de escritorio autónoma para Mac — 2026-09-25:** se añadió un contenedor Electron con protocolo local seguro que sirve la compilación incluida en el paquete; al hacer clic en Ágora no hay que arrancar Vite ni otro servidor. Se generó `release/Agora-0.1.0-arm64.dmg` (Apple Silicon, 136 MB); `hdiutil verify` confirma el checksum y el `app.asar` contiene la interfaz sin rutas `material/`. `npm run build`, `node --check electron/main.cjs` correctos. El sistema registró Ágora como proceso abierto, aunque la inspección de accesibilidad de la ventana agotó tiempo. Firma del desarrollador desactivada porque `codesign` devolvió `errSecInternalComponent` en la llave del certificado. Falta confirmar visualmente la ventana y que abre desde Aplicaciones en Finder. La app de escritorio tiene IndexedDB separado: copia las preguntas/historial mediante JSON; la copia actual no incluye láminas psicotécnicas ni sesiones en curso.

**Contraseña repetida al abrir la app — 2026-09-25:** la interfaz no implementa contraseña. La causa probable era el cifrado del almacén de cookies de Chromium/Electron usando la llave del sistema; el paquete anterior además no tenía firma de distribución consistente. Se desactivó ese cifrado mediante Electron Fuse (Ágora solo usa IndexedDB para los datos de estudio; no guarda credenciales) y se regeneró el DMG. La verificación del fuse confirma `EnableCookieEncryption=0`; `hdiutil verify` pasa y el paquete no contiene `material/`. Pendiente que la usuaria abra el paquete actualizado y confirme que cesaron las solicitudes del llavero.

**Instalación de escritorio — 2026-09-25:** Ágora ya tiene manifiesto e iconos de PWA, registro de service worker, caché del shell y de recursos del mismo origen ya visitados, y un botón de instalación cuando el navegador ofrece esa opción. En Safari se muestra la ruta Archivo → Añadir al Dock. La caché excluye rutas `material/`; la compilación solo generó la app y sus recursos estáticos. `npm run build`, sintaxis del service worker y JSON del manifiesto comprobados. Pendiente instalarla y comprobar el icono/ventana en el Mac de la usuaria y validar el acceso en iPad/iPhone. La PWA necesita publicarse en HTTPS para instalación permanente; `localhost` se admite para pruebas. IndexedDB sigue ligado al navegador y al origen.

**Navegación en sesiones — 2026-09-25:** se corrigió que la navegación del menú no cambiara de pantalla mientras había un test activo. “Volver al inicio” y los accesos de navegación ahora pausan visualmente la sesión; en Inicio aparece “Retomar test”, conservando respuestas e índice actual. `npm run build` correcto. Pendiente comprobar el flujo en navegador con una sesión real.

**Privacidad:** `material/` ya está en la raíz. Su contenido no se ha inspeccionado ni importado. `.gitignore` lo excluye, Vite tiene una regla de denegación para servirlo y la compilación comprobada solo generó `dist/` y `dist/assets/`.

**Extensión de psicotécnicos — visor de láminas:** se añadió un espacio para PNG seleccionados desde la interfaz, con selección de área, lápiz, borrador, color, grosor, deshacer y borrado completo. Imágenes y trazos se almacenan en IndexedDB del navegador; no se incorporó contenido de `material/`.

- **Archivos:** `src/PsychSheets.tsx`, `src/psych-sheets.css`, `src/data/database.ts`, `src/App.tsx`, `PROYECTO.md` y `PLAN.md`.
- **Pruebas realizadas:** `npm run build` correcto. En el navegador local se comprobó navegación al bloque psicotécnico, apertura del visor y estado vacío con controles disponibles. La lista de láminas aparece vacía como corresponde.
- **Pendiente:** verificar visualmente dibujo, borrado y persistencia tras recargar. Falta revisar visualmente Mac/iPad/iPhone.

**Ajuste del visor y formatos — 2026-09-25:** se adaptó el estilo a la paleta clara, tipografía y controles de Ágora. El visor acepta PNG, JPG/JPEG, HEIC/HEIF y PDF multipágina; PDF.js presenta sus páginas y `heic-to` convierte HEIC a JPEG localmente en el navegador. Se comprobó en navegador la pantalla con una lámina ya seleccionada y el nuevo estilo. `npm run build` pasa y `npm audit --omit=dev --offline` informa cero vulnerabilidades; el aviso de alta severidad anterior correspondía al PDF.js previo, actualizado a 6.2.108. El módulo HEIC generado ocupa ~3 MB sin comprimir y se descarga solo si se selecciona HEIC. Importaciones nuevas de JPG, HEIC y PDF, anotación PDF y pruebas de persistencia siguen pendientes.

**Zoom del visor — 2026-09-25:** se añadieron controles para alejar, acercar (50–300 %) y restablecer al 100 %. El escalado aplica conjuntamente a la página y a la capa de dibujo; se ajustó el grosor del trazo para que se calcule respecto al tamaño visible. `npm run build` pasa. En el navegador comprobé que la lámina PNG aumenta al 110 % y vuelve a 100 %. Pendiente comprobar el zoom sobre un PDF real.

**Cuestionarios de personalidad — 2026-09-25:** PPV y Competea se separan del flujo de preguntas con clave. La importación de texto desde el apartado Personalidad crea un borrador por línea, aplica escala 1–4 para PPV o 1–7 para Competea según nombre de archivo y permite revisar el cuestionario antes de guardar. Las sesiones usan el orden de colección, conservan cada selección y no muestran aciertos/errores. El modelo admite sus escalas numéricas y mantiene validación de copias de seguridad para preguntas objetivas y cuestionarios. `npm run build` pasa. Falta probar la importación con los dos archivos reales y confirmar que los recuentos sean 217 y ~119; no se ha guardado ni cambiado contenido de preguntas.

**Vista de tabla para personalidad — 2026-09-25:** el cuestionario completo se presenta en filas con enunciado y columna lateral «Respuesta». Cada fila tiene un selector numérico, el contador muestra cuántas se han respondido y las selecciones se persisten en el progreso de sesión. La vista adapta la proporción de columnas a móvil; teoría y psicotécnicos mantienen su modo de una pregunta por pantalla. `npm run build` pasa.

**Importación Excel en personalidad — 2026-09-25:** detectado que CSV/XLSX se procesaban como test objetivo de teoría aunque se eligieran desde Personalidad; la vista previa quedaba con opciones y clave vacías. Ahora el bloque Personalidad convierte filas de CSV/XLS/XLSX en enunciados, busca la primera hoja no vacía en libros Excel y acepta también `.xls`. La compilación pasa; falta comprobar con el archivo de la usuaria porque no está seleccionado en la conversación.

**Separación de enunciado en PPV — 2026-09-25:** la vista previa real mostró que el lector estaba concatenando las celdas de toda la fila Excel: junto al enunciado aparecían la respuesta guardada y columnas de perfil. Ahora busca la columna titulada como enunciado/pregunta/statement y solo toma esa celda; las filas de resumen sin enunciado quedan fuera. No se modificó ni guardó el lote abierto. `npm run build` pasa; para verificar hay que cerrar esa vista previa antigua y volver a seleccionar el Excel.

**Flujo para contestar Personalidad — 2026-09-25:** se aclaró la vista de importación: primero guarda los enunciados al banco, y luego abre PPV/Competea para responder en la tabla. Al importar archivos con nombre PPV se limitan los registros a sus 217 ítems; Competea a 119 para excluir filas finales de resultados. `npm run build` pasa.

**Plantilla personal de respuestas — 2026-09-25:** Personalidad admite una respuesta de referencia numérica opcional por enunciado. La importación Excel/CSV detecta columnas tituladas como respuesta; la revisión permite elegirla manualmente. En la tabla se compara la respuesta del nuevo intento con la plantilla y el resultado muestra coincidencias, sin aplicar nota de aciertos. La clave de referencia se guarda en IndexedDB, intentos y copias de seguridad. Build pasa; falta validar una importación real que tenga encabezada la columna de respuestas y el recuento de coincidencias.

## Fases

- [x] **Fase 0 — Documentación y decisiones.** Creados `AGENTS.md`, `PROYECTO.md`, `PLAN.md` y este registro. Prueba: presencia y coherencia de los documentos comprobadas.
- [/] **Fase 1 — Base visual y persistencia.** React/TypeScript/Vite, navegación adaptable, stores de IndexedDB y configuración inicial. `npm run build` pasa. La pantalla de inicio abre en navegador; falta comprobar recarga y tamaños iPad/iPhone. Privacidad: `/material/` se excluye de Git y el servidor Vite lo deniega; no se incluye en `dist/`.
- [/] **Fase 2 — Importación estructurada.** CSV (coma o punto y coma), XLSX y TXT generan borradores editables; se valida clave, cuatro opciones, tema/categoría y colección antes de guardar. El TXT real `Promo 32 plano.txt` produjo 45 borradores sin errores; se contrastaron enunciados, opciones y claves, con cero diferencias. Se conserva la agrupación `Promoción 32`; el lote no se guardó. CSV/XLSX aún pendientes.
- [/] **Corrección del importador TXT.** Detectado en la revisión de un archivo seleccionado desde la interfaz que un TXT de promoción puede llevar preguntas seguidas en una misma línea y dejar la última con opciones en líneas separadas. Se añadió separación por grupos a/b/c/d y soporte de opciones en líneas propias, conservando el texto. `npm run build` pasa. Pendiente volver a seleccionar ese archivo en la interfaz para confirmar la vista previa; el borrador anterior tenía una sola pregunta y no se guardó.
- [/] **Alternativas incompletas en TXT.** A petición de la usuaria, la vista de revisión ahora reserva cuatro campos aunque el origen traiga solo parte de las opciones; el parser conserva las opciones presentes y deja vacías las ausentes, sin generarlas automáticamente. `npm run build` pasa. Las propuestas para completar manualmente las preguntas 33 y 43 aún no se han incorporado al borrador abierto ni al banco.
- [/] **Fase 3 — Importación documental.** PDF.js y Mammoth extraen PDF/DOCX hacia la misma revisión. Se conservan saltos de línea del PDF cuando el extractor los proporciona. Build correcto; orden y tablas requieren validación manual con documentos reales.
- [/] **Fase 4 — Consulta del banco.** Listado con búsqueda, filtros por tema y categoría, grupos de exámenes y los dos tests de personalidad. Inicio y navegación se comprobaron en navegador; el repositorio aún no tiene preguntas.
- [/] **Fase 5 — Sesiones de examen.** Test personalizado por bloque/tema/categoría/colección y cantidad, simulacro con tiempo y configuración, práctica de exámenes agrupados y guardado del progreso para recuperarlo al recargar. Build correcto; falta completar una sesión con un lote real.
- [/] **Fase 6 — Historial y errores.** Cada intento guarda respuestas y copia de las preguntas; resultados muestran clave y respuestas; se repiten solo respuestas incorrectas (no las omitidas). Build correcto; falta validación con intentos reales.
- [/] **Fase 7 — Respaldo.** Exportación/restauración JSON con validación de estructura y confirmación de sustitución. Build correcto; falta probar ciclo de exportación/restauración con datos de usuario.
- [/] **Fase 8 — Copia privada en Google Drive.** Pantalla de Ajustes, OAuth `drive.appdata`, guardado/restauración de stores y láminas. Build y control de privacidad correctos. Ya están habilitadas Drive API, las URLs autorizadas, el test user y la variable Actions `GOOGLE_CLIENT_ID`. Falta publicar y probar el ciclo entre dos dispositivos.

## Siguiente paso

Publicar los cambios preparados y comprobar el flujo Guardar en Drive → Restaurar en otro dispositivo. También quedan pendientes pruebas de importaciones y validación de tamaños descritas arriba.

## Bloqueos

El banco local sigue sin preguntas importadas. CSV/XLSX y PDF/DOCX aún deben contrastarse desde archivos seleccionados por la persona usuaria y pasar por revisión antes de guardarse.

**Publicación PWA en GitHub Pages — 2026-09-25:** se adaptaron recursos, manifiesto y service worker a la ruta `/agora-estudio/`. El workflow `.github/workflows/deploy-pages.yml` compila y publica solo `dist/`, y falla si detecta `dist/material`. `.gitignore` excluye `/material/`, `.env*` y `.DS_Store`. El usuario autorizó hacer público el repositorio porque el plan actual no admite Pages desde repositorios privados. Repositorio: `https://github.com/noelia503-oss/agora-estudio`. Web HTTPS publicada: `https://noelia503-oss.github.io/agora-estudio/`. La ejecución 36111668355 terminó correctamente; el paso de control de privacidad pasó. Se comprobó que `material/` no está versionada ni aparece en `dist/`; no se leyó su contenido. Pendiente: abrir la URL en Safari del iPad y añadirla a la pantalla de inicio. Los datos de IndexedDB no se sincronizan entre Mac e iPad.

**Fase 8 — Copia privada en Google Drive — 2026-09-25:** añadida copia/restauración manual mediante OAuth de Google con alcance limitado a `drive.appdata`; IndexedDB sigue siendo el almacenamiento de trabajo. La copia contempla preguntas, colecciones, historial, ajustes, sesión en curso y láminas con trazos; los binarios usan Drive resumable upload. Restaurar pide confirmación antes de reemplazar los datos locales. No se guarda token OAuth ni se añade servidor o dependencia. En Google Cloud ya está habilitada Drive API, el cliente web `Agora web` autoriza la URL Pages y localhost, y la cuenta de uso se añadió como test user. El Client ID público se guardó en Actions como `GOOGLE_CLIENT_ID`. `npm run build` pasa y se comprobó que `material/` sigue ignorada por Git, no versionada y fuera de `dist/`. Pendiente publicar los cambios del repositorio y probar guardar/restaurar entre dispositivos. La app de escritorio `agora://` sigue usando copia JSON local.
